---
phase: 04-instructions-page
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/instructions/page.tsx
autonomous: false
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05

must_haves:
  truths:
    - "The Instructions page renders all 5 section headings with amber left-border accents"
    - "The 'Your Deliverables' section shows two numbered items with indigo circle badges"
    - "The Guidance callout is visually distinct from body text (amber-wash background, amber left border)"
    - "All 13 InstructionsPage tests pass (GREEN state)"
    - "The page content is verbatim from AGENT_PLAN.md — no paraphrasing"
    - "No 'use client' directive — pure Server Component"
  artifacts:
    - path: "src/app/instructions/page.tsx"
      provides: "Full Instructions page with all 6 content sections"
      min_lines: 80
      exports: ["default InstructionsPage"]
  key_links:
    - from: "src/app/instructions/page.tsx"
      to: "tailwind.config.ts"
      via: "Tailwind utility classes: crowe-indigo-dark, crowe-amber, tint-700, tint-900, bg-section-amber"
      pattern: "text-crowe-indigo-dark|border-crowe-amber|bg-section-amber"
    - from: "src/app/layout.tsx"
      to: "src/app/instructions/page.tsx"
      via: "Next.js children routing — layout max-w-7xl outer, page max-w-3xl inner"
      pattern: "max-w-3xl"
---

<objective>
Replace the 6-line Instructions page stub with the full case brief content and branded styling (TDD GREEN state).

Purpose: Participants can read the complete case brief — all 5 content sections plus the Guidance callout — with clear visual hierarchy and Crowe brand styling.
Output: src/app/instructions/page.tsx fully implemented; all 13 tests green; human visual verification of amber accents and callout styling.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-instructions-page/04-CONTEXT.md
@.planning/phases/04-instructions-page/04-RESEARCH.md
@.planning/phases/04-instructions-page/04-01-SUMMARY.md

<interfaces>
<!-- Layout contract the executor must nest inside. DO NOT modify layout.tsx. -->

From src/app/layout.tsx (outer wrapper — already exists, do not change):
```tsx
<main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  {children}   {/* InstructionsPage renders here */}
</main>
```

From tailwind.config.ts (all tokens already configured):
```
text-crowe-indigo-dark   → #011E41
border-crowe-amber       → #F5A800
text-crowe-amber         → #F5A800
bg-section-amber         → #fff8eb   (use this token, not bg-[#fff8eb])
text-tint-700            → #545968
text-tint-900            → #2d3142
```

From src/__tests__/InstructionsPage.test.tsx (tests written in Plan 01 — implementation must satisfy these):
- getByRole('heading', { name: 'Background' }) — must be an h2
- getByRole('heading', { name: 'Your Dataset' }) — must be an h2
- getByRole('heading', { name: 'Your Deliverables' }) — must be an h2
- getByRole('heading', { name: 'Tools You Should Use' }) — must be an h2
- getByRole('heading', { name: 'What You Do NOT Need to Do' }) — must be an h2
- getByText('Standalone HTML Dashboard') — deliverable 1 title
- getByText('2-Page Findings Memo') — deliverable 2 title
- getByText('1') and getByText('2') — badge numbers
- getByText('Guidance') — callout label (DOM text; CSS uppercase is visual only)
- getByText(/end deliverables are clear/i) — callout quote
- getByText(/use AI aggressively/i) — callout quote
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement InstructionsPage with full verbatim content and branded styling</name>
  <files>src/app/instructions/page.tsx</files>
  <action>
Replace the current 6-line stub at src/app/instructions/page.tsx with the full implementation. This is a named-export Server Component — NO 'use client' directive.

**CRITICAL: All content strings below are VERBATIM and LOCKED. Do not paraphrase, summarize, or alter wording.**

**Structure** (outer wrapper `max-w-3xl mx-auto py-12 sm:py-16`):

1. PAGE TITLE BLOCK (`mb-10 sm:mb-12`):
   - `h1` with `text-4xl font-bold text-crowe-indigo-dark`: "Instructions"
   - `p` with `text-tint-700 mt-2 text-lg`: "Case Brief"

2. BACKGROUND SECTION (`mt-10 sm:mt-12`):
   - `h2` with `text-2xl font-bold text-crowe-indigo-dark border-l-4 border-crowe-amber pl-4`: "Background"
   - `p` with `mt-4 text-tint-700 leading-relaxed`:
     "You are working with a fictional financial institution called Meridian Financial. The compliance team has been manually reviewing transaction records and flagging anomalies. They&apos;ve asked for a dashboard that makes it easier to visualize suspicious activity, understand patterns, and communicate findings to leadership."

3. YOUR DATASET SECTION (`mt-10 sm:mt-12`):
   - `h2` (same classes as above): "Your Dataset"
   - `p` with `mt-4 text-tint-700 leading-relaxed`:
     "You&apos;ve been provided with a synthetic dataset of approximately 500–1,000 transaction records (`transactions.csv`). Each row represents a single transaction. The compliance team has already reviewed every record — anomalies are pre-labeled in the `is_anomalous` column. A plain-English reason for each flagged transaction is in the `anomaly_notes` column."
   - Render backtick-wrapped filenames as `<code className="text-sm bg-tint-100 px-1 py-0.5 rounded font-mono">transactions.csv</code>` etc. for readability (Claude's discretion — keeps code filenames visually distinct)

4. YOUR DELIVERABLES SECTION (`mt-10 sm:mt-12`):
   - `h2` (same classes): "Your Deliverables"
   - `ol` with `space-y-6 mt-4`:
     - Item 1: flex row `gap-4 items-start`
       - Badge: `w-8 h-8 rounded-full bg-crowe-indigo-dark text-white flex items-center justify-center text-sm font-bold flex-shrink-0 mt-0.5` containing "1"
       - Content div:
         - `p` with `font-semibold text-crowe-indigo-dark`: "Standalone HTML Dashboard"
         - `p` with `text-tint-700 mt-1 leading-relaxed`: "A single .html file that can be opened directly in any browser with no server or dependencies. It should visualize the transaction data, surface anomalies, and allow a viewer to explore patterns."
     - Item 2: same structure
       - Badge: "2"
       - Title: "2-Page Findings Memo"
       - Description: "A written document (PDF, DOCX, or Markdown) summarizing what you found, what patterns you observed, and what you recommend. Pretend you are presenting this to the compliance team at Meridian Financial."

5. TOOLS YOU SHOULD USE SECTION (`mt-10 sm:mt-12`):
   - `h2` (same classes): "Tools You Should Use"
   - `ul` with `space-y-2 mt-4`:
     Four items, each as `li` with `flex gap-2 text-tint-700 leading-relaxed`:
     - Dash span: `text-crowe-amber font-bold flex-shrink-0 mt-0.5` containing "—"
     - Text span:
       - "VS Code (code editor)"
       - "Cursor or GitHub Copilot (AI coding assistant)"
       - "ChatGPT, Claude, or any LLM (for ideation, narrative writing, code generation)"
       - "Any free, publicly accessible UI libraries or chart libraries you find"

6. WHAT YOU DO NOT NEED TO DO SECTION (`mt-10 sm:mt-12`):
   - `h2` (same classes): "What You Do NOT Need to Do"
   - `ul` with `space-y-2 mt-4` (same bullet pattern):
     Four items:
     - "Build a machine learning model — anomalies are already labeled"
     - "Deploy anything to a server"
     - "Follow any specific branding guidelines"
     - "Use any particular framework — plain HTML/CSS/JS is perfectly valid"

7. GUIDANCE CALLOUT (`mt-10 sm:mt-12`):
   - `div` with `bg-section-amber border-l-4 border-crowe-amber rounded-r-lg px-6 py-5`
   - Label `p` with `text-sm font-semibold text-crowe-amber uppercase tracking-wide mb-3`: "Guidance"
   - Quote `p` with `italic text-tint-900 leading-relaxed`:
     "The end deliverables are clear. How you get there is entirely up to you. We encourage you to use AI aggressively — to ideate, to write code, to draft your memo, to debug. What we&apos;re evaluating is how well you can direct AI to produce something meaningful, polished, and insightful."

**Anti-patterns to avoid:**
- Do NOT add 'use client' — no interactivity, no hooks
- Do NOT use shadcn Card for sections — inline Tailwind only
- Do NOT use prose plugin — direct utilities
- Do NOT use -mx-4 negative margins — content stays in its column
- Do NOT add hr elements — use spacing only
- Do NOT use bg-[#fff8eb] arbitrary value — use bg-section-amber token instead
- Remember flex-shrink-0 on BOTH the badge span AND the dash span
- Use mt-0.5 on the badge span for optical alignment with the first text line
- Apostrophes in JSX text must be &apos; (e.g., "You&apos;ve", "They&apos;ve", "we&apos;re")
- Spacing token: mt-10 sm:mt-12 before each section h2; mt-4 for body after heading

Run tests after implementation:
`npx vitest run src/__tests__/InstructionsPage.test.tsx`
All 13 tests must pass (GREEN state).

Commit when GREEN: `git commit -m "feat(04-02): implement InstructionsPage with full case brief content"`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/InstructionsPage.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/app/instructions/page.tsx implements all 7 content blocks with verbatim text
    - All 13 InstructionsPage tests pass (GREEN)
    - No 'use client' directive in the file
    - Committed with message "feat(04-02): implement InstructionsPage with full case brief content"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Instructions page styling</name>
  <files>src/app/instructions/page.tsx</files>
  <action>
Human visual verification of the implemented InstructionsPage. Run `npm run dev` and open http://localhost:3000/instructions to check all visual styling that cannot be tested in jsdom.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Human confirms amber left-border headings, indigo circle badges, amber-wash callout, and narrow centered column render correctly. Full test suite is green.</done>
  <what-built>
    InstructionsPage at /instructions — full case brief with:
    - Large "Instructions" h1 + "Case Brief" subtitle
    - 5 sections each with amber left-border h2 headings
    - "Your Deliverables" with two items showing indigo circle badges (1, 2)
    - "Tools You Should Use" and "What You Do NOT Need to Do" with amber-dash bullet lists
    - "Guidance" callout panel with amber-wash background and amber left border
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/instructions
    2. Verify the navbar "Instructions" link is highlighted (active state from Phase 2)
    3. Confirm all 5 section headings have a visible amber (gold) left-border bar
    4. Confirm "Your Deliverables" shows two items with circular indigo badges numbered 1 and 2
    5. Confirm the amber-dash (—) bullet style on both list sections
    6. Confirm the "Guidance" callout at the bottom has a distinct warm-amber background panel with a left amber border
    7. Confirm page content is in a narrow centered column — not full-width
    8. On a mobile viewport, confirm the narrow column still reads comfortably (no horizontal overflow)
    9. Run `npx vitest run` — full suite must be green (Navbar + HomePage + InstructionsPage)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full test suite after Plan 02:
- `npx vitest run` — all tests green across Navbar, HomePage, InstructionsPage
- `npm run typecheck` — no TypeScript errors
- `npm run build` — production build succeeds
</verification>

<success_criteria>
- src/app/instructions/page.tsx is fully implemented with verbatim content from AGENT_PLAN.md
- All 13 InstructionsPage tests pass
- Full test suite (`npx vitest run`) is green
- Visual: amber left-border section headings, indigo circle badges, amber-wash callout panel
- No 'use client' directive
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-instructions-page/04-02-SUMMARY.md`
</output>
