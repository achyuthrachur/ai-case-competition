---
phase: 04-instructions-page
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/__tests__/InstructionsPage.test.tsx
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05

must_haves:
  truths:
    - "InstructionsPage test file exists at src/__tests__/InstructionsPage.test.tsx"
    - "All 13 tests are in RED state (fail because the stub page has no real content)"
    - "Tests cover all 5 requirements: INST-01 through INST-05"
    - "No new mocks are needed — InstructionsPage is a pure Server Component"
  artifacts:
    - path: "src/__tests__/InstructionsPage.test.tsx"
      provides: "Full test suite covering INST-01 through INST-05"
      min_lines: 60
  key_links:
    - from: "src/__tests__/InstructionsPage.test.tsx"
      to: "src/app/instructions/page.tsx"
      via: "import InstructionsPage from '@/app/instructions/page'"
      pattern: "import InstructionsPage"
---

<objective>
Write the failing test suite for the Instructions page (TDD RED state).

Purpose: Establish verifiable acceptance criteria before implementation. All 13 tests must fail against the current 6-line stub — proving the suite correctly detects missing content before Plan 02 adds it.
Output: src/__tests__/InstructionsPage.test.tsx with 13 tests across INST-01 through INST-05, all in RED state.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-instructions-page/04-CONTEXT.md
@.planning/phases/04-instructions-page/04-RESEARCH.md
@.planning/phases/04-instructions-page/04-VALIDATION.md

<interfaces>
<!-- Established test patterns from Phase 2/3. Executor should follow exactly. -->

From src/__tests__/HomePage.test.tsx (established pattern):
```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
// No mocks needed for Server Components with no hooks or external deps
```

From vitest.config.ts (already configured):
- environment: jsdom
- setupFiles: ['./src/__tests__/setup.ts']
- @/* alias resolves to ./src

From src/app/instructions/page.tsx (current stub — 6 lines, no real content):
- export default function InstructionsPage() { return <div>Instructions</div>; }
- No 'use client' directive
- No mocks needed (no next/link, no animation components, no iconsax icons)
</interfaces>
</context>

<feature>
  <name>InstructionsPage test suite</name>
  <files>src/__tests__/InstructionsPage.test.tsx</files>
  <behavior>
    INST-01 — Background section:
    - getByRole('heading', { name: 'Background' }) must find an h2
    - getByText(/Meridian Financial/) must find the background paragraph
    - getByText(/compliance team/i) must find the compliance team reference

    INST-02 — Your Dataset section:
    - getByRole('heading', { name: 'Your Dataset' }) must find an h2
    - getByText(/transactions\.csv/i) must find the dataset reference

    INST-03 — Your Deliverables section:
    - getByRole('heading', { name: 'Your Deliverables' }) must find an h2
    - getByText('Standalone HTML Dashboard') must find the first deliverable title
    - getByText('2-Page Findings Memo') must find the second deliverable title
    - getByText('1') must find badge for item 1
    - getByText('2') must find badge for item 2

    INST-04 — Tools and Not-Needed sections:
    - getByRole('heading', { name: 'Tools You Should Use' }) must find an h2
    - getByText(/VS Code/) must find a tools list item
    - getByText(/Cursor or GitHub Copilot/) must find a tools list item
    - getByRole('heading', { name: 'What You Do NOT Need to Do' }) must find an h2
    - getByText(/machine learning model/i) must find a not-needed list item
    - getByText(/Deploy anything/i) must find a not-needed list item

    INST-05 — Guidance callout:
    - getByText('Guidance') must find the callout label (DOM text; Tailwind uppercase is CSS-only)
    - getByText(/end deliverables are clear/i) must find the callout quote
    - getByText(/use AI aggressively/i) must find the key phrase in the callout

    RED state verification: Run `npx vitest run src/__tests__/InstructionsPage.test.tsx` and confirm ALL tests fail.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing InstructionsPage test suite (RED state)</name>
  <files>src/__tests__/InstructionsPage.test.tsx</files>
  <action>
Create src/__tests__/InstructionsPage.test.tsx with 13 tests covering INST-01 through INST-05. No mocks are needed — InstructionsPage is a pure Server Component with no hooks, router calls, animation dependencies, or icon imports.

Import pattern (matches HomePage.test.tsx):
```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import InstructionsPage from '@/app/instructions/page';
```

Write exactly these 13 tests (organized by requirement):

INST-01 (2 tests):
1. renders Background section heading — `getByRole('heading', { name: 'Background' })`
2. renders Background paragraph text — `getByText(/Meridian Financial/)` and `getByText(/compliance team/i)`

INST-02 (2 tests):
3. renders Your Dataset section heading — `getByRole('heading', { name: 'Your Dataset' })`
4. renders Your Dataset paragraph text — `getByText(/transactions\.csv/i)`

INST-03 (3 tests):
5. renders Your Deliverables section heading — `getByRole('heading', { name: 'Your Deliverables' })`
6. renders both numbered deliverable items — `getByText('Standalone HTML Dashboard')` and `getByText('2-Page Findings Memo')`
7. renders indigo number badges 1 and 2 — `getByText('1')` and `getByText('2')`

INST-04 (4 tests):
8. renders Tools You Should Use section heading — `getByRole('heading', { name: 'Tools You Should Use' })`
9. renders Tools list items — `getByText(/VS Code/)` and `getByText(/Cursor or GitHub Copilot/)`
10. renders What You Do NOT Need to Do section heading — `getByRole('heading', { name: 'What You Do NOT Need to Do' })`
11. renders Not-needed list items — `getByText(/machine learning model/i)` and `getByText(/Deploy anything/i)`

INST-05 (2 tests):
12. renders Guidance callout label — `getByText('Guidance')` (exact DOM text, NOT 'GUIDANCE' — Tailwind `uppercase` is CSS only)
13. renders Guidance quote text — `getByText(/end deliverables are clear/i)` and `getByText(/use AI aggressively/i)`

After writing the file, run the test suite. ALL 13 tests must fail (RED state) because the current stub returns only `<div>Instructions</div>`. If any test passes, the test is too permissive — tighten it.

Commit when RED: `git commit -m "test(04-01): add failing InstructionsPage test suite (INST-01..05)"`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/InstructionsPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/__tests__/InstructionsPage.test.tsx exists with 13 tests
    - All 13 tests FAIL (RED state) — confirmed by vitest output showing 13 failed, 0 passed
    - Test is committed with message "test(04-01): add failing InstructionsPage test suite (INST-01..05)"
  </done>
</task>

</tasks>

<verification>
Run after task commit:
- `npx vitest run src/__tests__/InstructionsPage.test.tsx` — must show 13 failed, 0 passed
- `npx vitest run` — full suite: only InstructionsPage tests fail; Navbar and HomePage tests remain green
</verification>

<success_criteria>
- src/__tests__/InstructionsPage.test.tsx exists and covers all 5 requirements
- All 13 tests are RED (fail against the stub)
- No existing tests regressed (Navbar and HomePage suites still green)
- Committed and ready for Plan 02 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-instructions-page/04-01-SUMMARY.md`
</output>
