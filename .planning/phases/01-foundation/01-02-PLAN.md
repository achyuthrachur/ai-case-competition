---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - .env.example
  - .env.local
  - .gitignore
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - FOUND-05
user_setup: []

must_haves:
  truths:
    - "Tailwind utility classes bg-crowe-indigo-dark, text-crowe-amber, shadow-crowe-card resolve without build errors"
    - "The page background is warm off-white (#f8f9fc) — not pure white, not dark"
    - "Font CSS variables (--font-display, --font-body, --font-mono) are declared in globals.css with Helvetica Now fallback stack"
    - ".env.example exists at the repo root with BLOB_READ_WRITE_TOKEN= key"
    - ".env.local is gitignored (git check-ignore confirms)"
    - "npm run build exits with code 0 after all token changes"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Full Crowe brand color palette + shadow + backgroundColor extensions"
      contains: "crowe-indigo-dark"
    - path: "src/app/globals.css"
      provides: "shadcn CSS variables overridden with Crowe HSL palette + font CSS variables"
      contains: "font-display"
    - path: "src/app/layout.tsx"
      provides: "Root layout applying font fallback stack and warm page background"
      contains: "Helvetica"
    - path: "src/app/page.tsx"
      provides: "Minimal placeholder page using Crowe design tokens (smoke test for token availability)"
      contains: "bg-crowe-indigo-dark"
    - path: ".env.example"
      provides: "Committed environment variable template"
      contains: "BLOB_READ_WRITE_TOKEN="
    - path: ".env.local"
      provides: "Local development env file (NOT committed)"
      contains: "BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/page.tsx"
      via: "Tailwind content scan"
      pattern: "content:.*src/\\*\\*"
    - from: "src/app/globals.css"
      to: "shadcn components"
      via: "CSS variable cascade"
      pattern: "--primary:.*215.*98%.*13%"
    - from: ".gitignore"
      to: ".env.local"
      via: "gitignore entry"
      pattern: "\\.env\\.local|\\.env\\*\\.local"
---

<objective>
Apply the full Crowe brand design system to the scaffolded Next.js project: replace tailwind.config.ts with the complete Crowe color/shadow/font token set from CLAUDE.md Section 2.2, override globals.css with Crowe-themed shadcn CSS variables (HSL format) from CLAUDE.md Section 4.2, configure the root layout with the Helvetica Now fallback font stack, write a minimal token-smoke-test page, and create the required environment files.

Purpose: Every later phase depends on Tailwind utilities like bg-crowe-indigo-dark and shadow-crowe-card being available. Setting this up now means Phase 2+ can use tokens immediately without configuration work.
Output: tailwind.config.ts with full Crowe palette, globals.css with Crowe shadcn overrides + font vars, layout.tsx with fallback font, .env.example and .env.local created, and a passing npm run build.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Token values are AUTHORITATIVE — copied from CLAUDE.md Sections 2.2, 4.2. Do not deviate. -->

## tailwind.config.ts — Crowe token extension block (CLAUDE.md Section 2.2)

The ENTIRE theme.extend block must be replaced with this. Do not keep create-next-app's generated colors:

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        crowe: {
          amber: { bright: '#FFD231', DEFAULT: '#F5A800', dark: '#D7761D' },
          indigo: { bright: '#003F9F', DEFAULT: '#002E62', dark: '#011E41' },
          teal: { bright: '#16D9BC', DEFAULT: '#05AB8C', dark: '#0C7876' },
          cyan: { light: '#8FE1FF', DEFAULT: '#54C0E8', dark: '#007DA3' },
          blue: { light: '#32A8FD', DEFAULT: '#0075C9', dark: '#0050AD' },
          violet: { bright: '#EA80FF', DEFAULT: '#B14FC5', dark: '#612080' },
          coral: { bright: '#FF526F', DEFAULT: '#E5376B', dark: '#992A5C' },
        },
        tint: {
          950: '#1a1d2b', 900: '#2d3142', 700: '#545968', 500: '#8b90a0',
          300: '#c8cbd6', 200: '#dfe1e8', 100: '#eef0f4', 50: '#f6f7fa',
        },
      },
      fontFamily: {
        display: ['Helvetica Now Display', 'Helvetica Neue', 'Arial', 'system-ui', 'sans-serif'],
        body: ['Helvetica Now Text', 'Helvetica Neue', 'Arial', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'Fira Code', 'IBM Plex Mono', 'Consolas', 'monospace'],
      },
      boxShadow: {
        'crowe-sm': '0 1px 3px rgba(1,30,65,0.06), 0 1px 2px rgba(1,30,65,0.04)',
        'crowe-md': '0 4px 8px -2px rgba(1,30,65,0.06), 0 2px 4px -1px rgba(1,30,65,0.04)',
        'crowe-lg': '0 6px 16px -4px rgba(1,30,65,0.07), 0 4px 6px -2px rgba(1,30,65,0.04)',
        'crowe-xl': '0 12px 32px -8px rgba(1,30,65,0.08), 0 8px 16px -4px rgba(1,30,65,0.03)',
        'crowe-hover': '0 8px 24px -4px rgba(1,30,65,0.10), 0 4px 8px -2px rgba(1,30,65,0.04)',
        'crowe-card': '0 1px 3px rgba(1,30,65,0.04), 0 6px 16px rgba(1,30,65,0.04), 0 12px 32px rgba(1,30,65,0.02)',
        'amber-glow': '0 4px 16px rgba(245,168,0,0.20)',
      },
      backgroundColor: {
        'page': '#f8f9fc',
        'section': '#f6f7fa',
        'section-warm': '#f0f2f8',
        'section-amber': '#fff8eb',
      },
    },
  },
  plugins: [],
};

export default config;
```

## globals.css — Crowe shadcn override block (CLAUDE.md Section 4.2)

After the Tailwind directives, replace the entire :root block that shadcn generated with:

```css
@layer base {
  :root {
    --background: 225 33% 98%;
    --foreground: 228 20% 22%;
    --card: 225 50% 99%;
    --card-foreground: 228 20% 22%;
    --popover: 0 0% 100%;
    --popover-foreground: 228 20% 22%;
    --primary: 215 98% 13%;
    --primary-foreground: 225 33% 97%;
    --secondary: 39 100% 48%;
    --secondary-foreground: 215 98% 13%;
    --muted: 225 20% 96%;
    --muted-foreground: 228 10% 37%;
    --accent: 39 100% 48%;
    --accent-foreground: 215 98% 13%;
    --destructive: 341 79% 56%;
    --destructive-foreground: 225 33% 97%;
    --border: 226 17% 89%;
    --input: 226 17% 89%;
    --ring: 215 100% 19%;
    --radius: 0.75rem;

    /* Font tokens — fallback stack (no .woff2 available) */
    --font-display: Arial, 'Helvetica Neue', Helvetica, system-ui, sans-serif;
    --font-body: Arial, 'Helvetica Neue', Helvetica, system-ui, sans-serif;
    --font-mono: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
  }
}
```

CRITICAL: If shadcn generated `oklch(...)` values in the :root block, delete that entire block and replace with the HSL version above. Do NOT mix OKLCH and HSL.

## layout.tsx — font and background

No `next/font/local` call needed (no .woff2 files). Apply font via inline style or className on body:

```typescript
// src/app/layout.tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Meridian Financial — AI Case Competition',
  description: 'Competition portal for the Meridian Financial AI Case Competition',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body
        className="bg-page"
        style={{ fontFamily: "Arial, 'Helvetica Neue', Helvetica, system-ui, sans-serif" }}
      >
        {children}
      </body>
    </html>
  );
}
```

NOTE: export default is acceptable for Next.js layout/page files — these are framework entry points, not components. The named-export rule applies to components in src/components/.

## page.tsx — token smoke test

Replace the create-next-app default page with this minimal placeholder that uses Crowe tokens (so Tailwind purging validates the token classes):

```typescript
// src/app/page.tsx
export default function HomePage() {
  return (
    <main className="min-h-screen bg-page flex items-center justify-center">
      <div className="bg-white rounded-xl shadow-crowe-card p-8 max-w-md">
        <h1 className="text-3xl font-bold text-crowe-indigo-dark">
          Meridian Financial — AI Case Competition
        </h1>
        <p className="mt-4 text-tint-700">Design system active. Phase 1 complete.</p>
        <div className="mt-4 h-1 w-16 bg-crowe-amber rounded-full" />
      </div>
    </main>
  );
}
```

export default is acceptable here — this is a Next.js page entry point, not a component.

## Environment files

.env.example (committed to repo):
```
BLOB_READ_WRITE_TOKEN=
```

.env.local (NOT committed — gitignored):
```
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here
```

Verify .gitignore contains .env.local — create-next-app@15 adds this by default. Check before creating .env.local.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace tailwind.config.ts with full Crowe brand token set</name>
  <files>tailwind.config.ts</files>
  <action>
Overwrite the create-next-app-generated tailwind.config.ts with the exact content from the interfaces block above. The full config defines:
- crowe color namespace (amber, indigo, teal, cyan, blue, violet, coral — all 3 shades each)
- tint neutral namespace (950 through 50)
- fontFamily extensions (display, body, mono)
- boxShadow extensions (crowe-sm through crowe-xl, crowe-hover, crowe-card, amber-glow)
- backgroundColor extensions (page, section, section-warm, section-amber)
- content: ['./src/**/*.{ts,tsx}'] — must scan src/ not root

Use the EXACT token values from the interfaces block. Do not approximate colors.

After writing the file, run npm run build to verify Tailwind can parse the config and that the content glob is correct (a Tailwind config error will fail the build).
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/AI Case Competition" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>tailwind.config.ts contains all crowe.* and tint.* color keys, all 7 shadow extensions, all 4 backgroundColor extensions. npm run build exits with code 0. The class bg-crowe-indigo-dark is referenced in page.tsx and survives Tailwind purging (present in the built output).</done>
</task>

<task type="auto">
  <name>Task 2: Override globals.css with Crowe HSL shadcn variables and font tokens, then update layout.tsx</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
Open src/app/globals.css. It currently has the shadcn-generated @layer base { :root { ... } } block. Replace the entire :root block with the Crowe HSL version from the interfaces section. The file structure after editing must be:

```
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    [Crowe HSL variables exactly as specified in interfaces block]
    [Font CSS variables at the end of :root]
  }
}
```

If shadcn generated dark mode variables (e.g., .dark { ... }), leave that block in place — do not delete it, even if unused. Only replace the :root block contents.

If the generated globals.css uses oklch() values anywhere in :root, confirm they are fully replaced with the HSL values above before saving.

Then update src/app/layout.tsx to match the pattern in the interfaces block — remove any create-next-app geist font import, apply the Helvetica fallback stack via inline style on body, and apply bg-page class.

The metadata title must be exactly: 'Meridian Financial — AI Case Competition'
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/AI Case Competition" && grep "font-display" src/app/globals.css && grep "Helvetica" src/app/globals.css && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>globals.css :root block contains --font-display, --font-body, --font-mono variables pointing to Helvetica/Arial fallback stacks. globals.css :root block contains --primary: 215 98% 13% (Crowe Indigo Dark in HSL). No oklch() values remain in :root. layout.tsx imports globals.css, sets metadata title to "Meridian Financial — AI Case Competition", and applies fontFamily inline style with Arial/'Helvetica Neue' fallback. npm run typecheck exits with code 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create environment files and verify gitignore coverage</name>
  <files>.env.example, .env.local, .gitignore</files>
  <action>
First, check whether .gitignore already contains .env.local (create-next-app@15 adds this by default):

```bash
grep -n "env" .gitignore
```

If .env.local is not present in .gitignore, add these two lines to .gitignore:
```
.env.local
.env*.local
```

Create .env.example at the project root with exactly this content (the key with no value — safe to commit):
```
BLOB_READ_WRITE_TOKEN=
```

Create .env.local at the project root with:
```
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here
```

After creating both files, run the verification commands to confirm .env.local is gitignored and .env.example has the correct key.

Note: This is a git repository only if git is initialized. If git is not yet initialized (no .git directory), the git check-ignore command will fail — that is acceptable. The important thing is that .gitignore contains the entry so when git is initialized, the file will be ignored.
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/AI Case Competition" && ls .env.example && grep "BLOB_READ_WRITE_TOKEN" .env.example && grep "env.local" .gitignore</automated>
  </verify>
  <done>.env.example exists at project root and contains "BLOB_READ_WRITE_TOKEN=" with no value. .env.local exists at project root and contains "BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here". .gitignore contains ".env.local" or ".env*.local" entry.</done>
</task>

</tasks>

<verification>
Full plan verification after all three tasks complete:

```bash
# In: C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/AI Case Competition

# FOUND-03: Tailwind color tokens — build validates class usage
npm run build

# FOUND-03: Key token present in config
grep "crowe-indigo" tailwind.config.ts

# FOUND-03: Shadow tokens present
grep "crowe-card" tailwind.config.ts

# FOUND-04: Font CSS variables declared
grep "font-display" src/app/globals.css
grep "Helvetica" src/app/globals.css

# FOUND-04: No OKLCH leak in globals.css
grep "oklch" src/app/globals.css && echo "FAIL: oklch found" || echo "PASS: no oklch"

# FOUND-05: Environment files
ls .env.example
grep "BLOB_READ_WRITE_TOKEN" .env.example
grep "env.local" .gitignore

# Full typecheck
npm run typecheck
```

Expected: build exits 0, typecheck exits 0, grep commands find expected strings, no oklch in globals.css, .env.example contains the key.
</verification>

<success_criteria>
- tailwind.config.ts extends Tailwind with full crowe.* color palette (indigo, amber, teal, cyan, blue, violet, coral × 3 shades each)
- tailwind.config.ts extends with all 7 shadow utilities (crowe-sm, crowe-md, crowe-lg, crowe-xl, crowe-hover, crowe-card, amber-glow)
- tailwind.config.ts extends backgroundColor with page, section, section-warm, section-amber
- globals.css :root block uses HSL format (not oklch) — Crowe shadcn theme variables verbatim from CLAUDE.md Section 4.2
- globals.css :root contains --font-display, --font-body, --font-mono CSS variables pointing to Helvetica/Arial fallback stack
- src/app/layout.tsx title is "Meridian Financial — AI Case Competition"
- src/app/layout.tsx body has fontFamily inline style with Arial/'Helvetica Neue' fallback
- .env.example at project root contains "BLOB_READ_WRITE_TOKEN=" (committed value — no secret)
- .env.local at project root contains "BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here"
- .gitignore contains an entry matching .env.local
- npm run build and npm run typecheck both exit with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following the summary template.
</output>
