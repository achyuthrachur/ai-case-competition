---
phase: 07-dataset-generation
plan: 02
subsystem: data
tags: [nodejs, csv, synthetic-data, dataset, vitest]

# Dependency graph
requires:
  - phase: 07-01
    provides: generate-dataset.test.js test suite and scripts/ directory marker
provides:
  - scripts/generate-dataset.js — standalone Node.js script generating 75,000-row synthetic transaction CSV
  - public/transactions.csv — 9.2 MB synthetic financial dataset for competition participants
  - Updated Downloads page with accurate file size (~10-12 MB) and row count (~75,000)
affects: [08-submission-api, downloads-page]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Build full CSV string in memory via array join, single fs.writeFileSync call
    - Fisher-Yates shuffle before assigning sequential transaction IDs
    - Accounts table as module-level constant; historical_avg_amount and monthly_transaction_count never randomly generated per row
    - RFC 4180 csvEscape wrapper applied to all text fields
    - Structuring anomalies generated in groups of 3-5 with while loop + slice to hit exact count
    - Dormant accounts excluded from normal row generation; only appear in Sep-Dec window

key-files:
  created:
    - scripts/generate-dataset.js
    - public/transactions.csv
  modified:
    - src/app/downloads/page.tsx
    - src/__tests__/DownloadsPage.test.tsx

key-decisions:
  - "Single-pass CSV build: build full string in memory, write once — no appendFileSync loop"
  - "Anomaly notes use descriptive English with fingerprint keywords matching test assertions (historical average, sanctioned jurisdiction, structuring, no activity, transactions in)"
  - "DownloadsPage test '~150 KB' assertion updated to '~10-12 MB' — stale value from pre-dataset spec"

patterns-established:
  - "Dataset generation: accounts table as source of truth for per-account constants"
  - "Anomaly type fingerprints: each type produces a distinct keyword in anomaly_notes for test detectability"

requirements-completed: [DATA-01, DATA-02, DATA-03, DATA-04]

# Metrics
duration: 5min
completed: 2026-03-04
---

# Phase 7 Plan 02: Dataset Generation Implementation Summary

**75,000-row synthetic Meridian Financial transaction CSV (9.2 MB) generated by standalone Node.js script; all 8 DATA-0x tests green across 55 total passing tests**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-04T21:11:30Z
- **Completed:** 2026-03-04T21:15:55Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Implemented `scripts/generate-dataset.js` — 280-line Node.js script with zero external dependencies
- Generated `public/transactions.csv` — 75,000 rows, 9.2 MB, all 12 columns in locked order
- 20 accounts (ACC-101..ACC-120) with consistent names and historical averages; 3 designated dormant
- 5 anomaly types (7,500 total, ~10%): amount spike, high-risk jurisdiction, structuring, dormant activity, frequency spike
- Updated Downloads page: `~150 KB` → `~10–12 MB`, `~500–1,000 rows` → `~75,000 rows`
- All 55 Vitest tests pass across 6 test suites including the new generate-dataset.test.js suite

## Task Commits

1. **Task 1: Implement scripts/generate-dataset.js** - `e29bd71` (feat)
2. **Task 2: Update Downloads page file size + run full test suite GREEN** - `bebc5fb` (feat)

## Files Created/Modified

- `scripts/generate-dataset.js` — Standalone Node.js dataset generator, 280 lines, zero external deps
- `public/transactions.csv` — 75,000-row synthetic transaction dataset, 9.2 MB
- `src/app/downloads/page.tsx` — Updated fileSize to `~10–12 MB` and description to `~75,000 rows`
- `src/__tests__/DownloadsPage.test.tsx` — Updated stale `~150 KB` assertion to `~10–12 MB`

## Decisions Made

- Used `path.join(__dirname, '../public/transactions.csv')` as locked — avoids relative path resolution issues
- Called `fs.mkdirSync(path.dirname(OUTPUT_PATH), { recursive: true })` before writeFileSync as per pitfalls guide
- Structuring `generateStructuringPatterns(count)` uses while loop with `rows.length < count` guard and final `slice(0, count)` to return exactly `PER_TYPE` rows despite group-based generation
- Dormant activity distributes `count` rows across the 3 dormant accounts in small bursts to produce realistic clustering
- Frequency spike rows keep `historical_avg_amount` unchanged and only inflate `monthly_transaction_count` (per locked decision)
- Amount spike `ratio` computed with `toFixed(1)` so note string shows clean `5.3x` format

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated DownloadsPage test stale value assertion**
- **Found during:** Task 2 (run full test suite GREEN)
- **Issue:** `src/__tests__/DownloadsPage.test.tsx` line 36 asserted `'~150 KB'` for transactions.csv file size. After updating the Downloads page to `'~10–12 MB'`, this test failed with "Unable to find an element with the text: ~150 KB"
- **Fix:** Updated assertion from `getByText('~150 KB')` to `getByText('~10\u201312 MB')` to match the new correct value
- **Files modified:** `src/__tests__/DownloadsPage.test.tsx`
- **Verification:** All 55 tests pass including DownloadsPage suite
- **Committed in:** `bebc5fb` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - stale test assertion)
**Impact on plan:** Fix was necessary — the plan explicitly required changing the file size string, so the test needed to match. No scope creep.

## Issues Encountered

None — script ran correctly on first attempt with all verification criteria met.

## Self-Check

- [x] `scripts/generate-dataset.js` exists (280 lines)
- [x] `public/transactions.csv` exists (75,000 rows, 9.2 MB)
- [x] `src/app/downloads/page.tsx` contains `~10–12 MB`
- [x] All 55 Vitest tests pass (exit 0)
- [x] Task 1 commit: e29bd71
- [x] Task 2 commit: bebc5fb

## Self-Check: PASSED

## Next Phase Readiness

- Phase 7 complete: dataset file at `public/transactions.csv`, served as static asset via Next.js `/transactions.csv` route
- Phase 8 (Submission API) can proceed; requires `BLOB_READ_WRITE_TOKEN` from Vercel Blob store
- Downloads page fully functional with accurate file metadata

---
*Phase: 07-dataset-generation*
*Completed: 2026-03-04*
